# OSM Tools MCP Server Configuration

# Transport mode: stdio or http
OSM_TRANSPORT=stdio

# HTTP server port (only used when transport is http)
OSM_HTTP_PORT=3000

# User-Agent for API requests (required by OSM usage policies)
OSM_USER_AGENT=osm-tools-mcp/1.0.0

# Request throttle delay in milliseconds (respect API usage policies)
OSM_THROTTLE_MS=1000

# Nominatim API base URL
OSM_NOMINATIM_URL=https://nominatim.openstreetmap.org

# Overpass API base URL
OSM_OVERPASS_URL=https://overpass-api.de/api/interpreter

# OSRM API base URL
OSM_OSRM_URL=https://router.project-osrm.org
